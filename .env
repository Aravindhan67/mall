# Frontend Environment Variables
# NOTE: Since you are using vanilla HTML/JS without a bundler, the browser cannot read this file directly.
# To use these variables in your frontend, you would need to use a build tool like Vite or Webpack,
# or expose them via your backend server.

API_URL=http://localhost:3000
